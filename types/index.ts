export interface NavItem {
  label: string;
  path: string;
}

export interface Course {
  id: string;
  title: string;
  price: number;
  description: string;
}

export interface Testimonial {
  name: string;
  role: string;
  comment: string;
  image: string;
}